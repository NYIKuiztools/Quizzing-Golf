document.addEventListener("DOMContentLoaded", function() {
	manager.elements.buttons.start.addEventListener("click", function() {
		manager.buttons.start();
	})
	manager.elements.buttons.begin.addEventListener("click", function() {
		manager.buttons.begin();
	})
})